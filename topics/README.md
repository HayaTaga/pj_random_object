# Topics

研究テーマごとの作業場所です。

新しいテーマを始めるときは、次のコマンドで `topics/<theme>/` を作ります。

```sh
make topic TOPIC=random_sets
```

テーマ名は `snake_case` を基本にします。

最初に各テーマの `README.md` に次の情報だけ書いておくと、後から再開しやすくなります。

- 研究の問い
- 使うランダムオブジェクトの種類
- 想定している理論・推定・シミュレーションの位置づけ
- 現在のステータス
- 再現に必要なコマンド
